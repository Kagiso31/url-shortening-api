const CTA = () => {
  return (
    <section className="cta">
      <div className="container">
        <h2 className="cta-heading">Boost your links today</h2>
        <button className="btn btn-cta">Get Started</button>
      </div>
    </section>
  );
};

export default CTA;
